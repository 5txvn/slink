<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - <%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="min-h-screen flex items-center justify-center bg-gray-100 dark:bg-gray-900 font-sans">
    <div class="bg-white dark:bg-gray-800 rounded-2xl p-8 max-w-[90%] w-[480px] text-center shadow-[4px_4px_10px_#d1d1d1,-4px_-4px_10px_#ffffff] dark:shadow-[4px_4px_10px_#1a202c,-4px_-4px_10px_#4a5568]">
        <i class="fas fa-exclamation-circle text-[3rem] text-[#800000] mb-4"></i>
        <h1 class="text-[#800000] text-2xl font-semibold mb-4"><%= title %></h1>
        <p class="text-gray-600 dark:text-gray-300 mb-6 leading-relaxed"><%= message %></p>
        <p class="text-[#800000] font-medium mt-4">Redirecting in <span id="countdown">3</span> seconds...</p>
    </div>

    <script>
        $(document).ready(function() {
            let timeLeft = 3;
            const countdownElement = $('#countdown');
            
            const countdown = setInterval(() => {
                timeLeft--;
                countdownElement.text(timeLeft);
                
                if (timeLeft <= 0) {
                    clearInterval(countdown);
                    window.location.href = '<%= redirectUrl %>';
                }
            }, 1000);
        });
    </script>
</body>
</html> 