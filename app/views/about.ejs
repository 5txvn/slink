<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Slink | About</title>
    <link rel="icon" href="/images/slink-logo.png" type="image/x-icon">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        @keyframes gradient-flow {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }
        .gradient-text {
            background: linear-gradient(45deg, #800000, #cc0000);
            background-size: 200% 200%;
            animation: gradient-flow 3s ease-in-out infinite;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        .atom {
            position: absolute;
            color: #718096;
            opacity: 0.3;
            pointer-events: none;
        }
        @keyframes spin-slow {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }
        .animate-spin-slow {
            animation: spin-slow 8s linear infinite;
        }
        .hide-scrollbar {
            scrollbar-width: none;
            -ms-overflow-style: none;
        }
        .hide-scrollbar::-webkit-scrollbar {
            display: none;
        }
        .card-hover {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .card-hover:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
        }
        body {
            overflow: auto;
            scrollbar-width: none;
            -ms-overflow-style: none;
        }
        ::-webkit-scrollbar {
            display: none;
        }
    </style>
</head>
<body class="bg-gray-100 min-h-screen flex flex-col hide-scrollbar" style="overflow:auto;">
    <!-- Floating Atoms Background -->
    <div id="atoms-container"></div>
    
    <!-- Main Content -->
    <main class="flex-1 flex flex-col items-center px-4 py-8 md:py-12 relative z-10">
        <!-- About Section -->
        <div class="w-full max-w-[90vw] md:max-w-[80vw] lg:max-w-[70vw] bg-white rounded-2xl shadow-xl p-8 md:p-14 flex flex-col items-center card-hover mb-12">
            <h1 class="gradient-text text-4xl md:text-6xl lg:text-7xl font-extrabold mb-16 text-center">About Slink</h1>
            
            <!-- Creator Section -->
            <div class="flex flex-col md:flex-row items-center gap-8 mb-12 w-full">
                <div class="w-48 h-48 md:w-64 md:h-64 rounded-full overflow-hidden shadow-xl">
                    <img src="/images/headshot.png" alt="Creator Headshot" class="w-full h-full object-cover">
                </div>
                <div class="flex-1 text-center md:text-left">
                    <h2 class="text-3xl font-bold text-[#800000] mb-4">About the Creator 👨‍💻</h2>
                    <p class="text-gray-600 text-lg mb-4">Hey! My name is Steven Livingston and I'm a rising senior at SEM who created Slink to bring the SEM community closer togeter and connect current students with alumni for an enhanced college and career exploration experience. I've been coding since early middle school and share a deep passion for mathematics and computer science through the work that I do outside of school. Ultimately, I hope to continue using my passion to build platforms to help bring people together and reinforce connections between people. Feel free to reach out to me if you want to help contribute to Slink or work with me on other projects!</p>
                    <div class="flex justify-center md:justify-start gap-4">
                        <a href="https://github.com/5txvn" class="text-2xl text-[#800000] hover:text-[#cc0000] transition-colors">
                            <i class="fab fa-github"></i>
                        </a>
                        <a href="https://www.linkedin.com/in/steven-livingston-b599482b5/" class="text-2xl text-[#800000] hover:text-[#cc0000] transition-colors">
                            <i class="fab fa-linkedin"></i>
                        </a>
                    </div>
                </div>
            </div>

            <!-- Mission Section -->
            <div class="w-full mb-12">
                <h2 class="text-3xl font-bold text-[#800000] mb-6 text-center">Our Mission 🎯</h2>
                <p class="text-gray-600 text-lg text-center mb-8">Slink was created with a simple mission: to strengthen the SEM community by providing a modern, secure, and user-friendly platform for alumni to connect, share experiences, and support each other's growth.</p>
                
                <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                    <div class="bg-gray-50 rounded-xl p-6 card-hover">
                        <div class="text-4xl mb-4">🔒</div>
                        <h3 class="text-xl font-bold text-[#800000] mb-2">Privacy First</h3>
                        <p class="text-gray-600">Your data is protected with industry-standard security measures and strict privacy controls.</p>
                    </div>
                    <div class="bg-gray-50 rounded-xl p-6 card-hover">
                        <div class="text-4xl mb-4">🤝</div>
                        <h3 class="text-xl font-bold text-[#800000] mb-2">Community Focus</h3>
                        <p class="text-gray-600">Built specifically for the SEM community, fostering meaningful connections and relationships.</p>
                    </div>
                    <div class="bg-gray-50 rounded-xl p-6 card-hover">
                        <div class="text-4xl mb-4">💡</div>
                        <h3 class="text-xl font-bold text-[#800000] mb-2">Continuous Innovation</h3>
                        <p class="text-gray-600">Regular updates and new features based on community feedback and needs.</p>
                    </div>
                </div>
            </div>

            <!-- Contact Section -->
            <div class="w-full text-center">
                <h2 class="text-3xl font-bold text-[#800000] mb-6">Get in Touch 📬</h2>
                <p class="text-gray-600 text-lg mb-4">Have questions or suggestions? We'd love to hear from you!</p>
                <a href="mailto:steven.livingston.mathcomp@gmail.com" class="inline-block px-8 py-4 rounded-xl bg-[#800000] text-white text-lg font-bold shadow hover:bg-[#a94442] transition-all">
                    Contact Us
                </a>
            </div>
        </div>
    </main>

    <script>
        // Floating Atoms Animation
        $(document).ready(function() {
            function createAtom() {
                const atom = $('<i class="fas fa-atom atom"></i>');
                const container = $('#atoms-container');
                const containerWidth = $(window).width();
                const containerHeight = $(window).height();
                const size = window.innerWidth < 640 ? 'text-5xl' : window.innerWidth < 1024 ? 'text-6xl' : 'text-7xl';
                atom.addClass(size);
                let x = Math.random() * containerWidth;
                let y = Math.random() * containerHeight;
                const speed = 2;
                let vx = (Math.random() - 0.5) * speed;
                let vy = (Math.random() - 0.5) * speed;
                let rotation = Math.random() * 360;
                let rotationSpeed = (Math.random() - 0.5) * 2;
                atom.css({ left: x + 'px', top: y + 'px', transform: `rotate(${rotation}deg)` });
                container.append(atom);
                function animate() {
                    x += vx;
                    y += vy;
                    rotation += rotationSpeed;
                    if (x <= 0 || x >= containerWidth) { vx *= -1; x = x <= 0 ? 0 : containerWidth; }
                    if (y <= 0 || y >= containerHeight) { vy *= -1; y = y <= 0 ? 0 : containerHeight; }
                    atom.css({ left: x + 'px', top: y + 'px', transform: `rotate(${rotation}deg)` });
                    requestAnimationFrame(animate);
                }
                animate();
            }
            const numAtoms = 8;
            for (let i = 0; i < numAtoms; i++) { createAtom(); }
        });
    </script>
</body>
</html> 