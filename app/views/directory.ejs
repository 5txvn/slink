<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Slink | Directory</title>
    <link rel="icon" href="/images/slink-logo.png" type="image/x-icon">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body, html {
            overflow: auto;
            scrollbar-width: none;
            -ms-overflow-style: none;
        }
        body::-webkit-scrollbar,
        html::-webkit-scrollbar {
            display: none;
        }
        .dot-pattern {
            background-image: radial-gradient(rgba(96, 34, 52, 0.5) 1px, transparent 1px), radial-gradient(rgba(0, 0, 0, 0.5) 1px, transparent 1px);
            background-size: 20px 20px;
            background-position: 0 0, 10px 10px;
            background-color: #ffffff;
        }
        .modern-card {
            background: #fff;
            border-radius: 1.25rem;
            box-shadow: 0 4px 24px 0 rgba(60,0,0,0.08), 0 1.5px 8px #e0e0e0;
            transition: box-shadow 0.2s;
        }
        .modern-card:hover {
            box-shadow: 0 8px 32px 0 rgba(60,0,0,0.13), 0 2.5px 12px #e0e0e0;
        }
        .modern-input {
            background: #f7f7fa;
            border-radius: 0.75rem;
            border: 2px solid #e5e7eb;
            box-shadow: 0 1.5px 6px #ececec;
            transition: border 0.2s, box-shadow 0.2s;
        }
        .modern-input:focus {
            border: 2px solid #602234;
            box-shadow: 0 2px 12px #e0e0e0;
        }
        .input-icon {
            top: 50% !important;
            transform: translateY(-50%) !important;
        }
        .modal {
            max-width: 800px !important;
        }
        /* Neumorphic inner shadow for buttons */
        .neumorphic-btn {
            background: #f7f7f7;
            border: 1.5px solid #222;
            transition: all 0.2s ease;
            box-shadow: none !important;
        }
        .neumorphic-btn:active {
            transform: scale(0.95);
        }
        .neumorphic-btn, .close-button {
            box-shadow: inset 4px 4px 10px #c0bebe, inset 6px 6px 16px #e0e0e0 !important;
        }
        .close-button {
            background: #800000;
            color: #fff;
            border: none;
            border-radius: 0.375rem;
            font-weight: 500;
            transition: all 0.2s ease;
            box-shadow: inset 2px 2px 6px #5a0000, inset 3px 3px 8px #a94442;
        }
        .close-button:hover {
            background: #a94442;
        }
        .modal-content.nice-modal, .modal-content.neumorphic {
            box-shadow: inset 4px 4px 12px #e0e0e0, inset 8px 8px 18px #f7f7f7;
        }
        .directory-title {
            background-size: 200% 200%;
            animation: gradient-flow 3s ease-in-out infinite;
        }
        @keyframes gradient-flow {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }
        /* Table header icon colors */
        .icon-user { color: #602234; }
        .icon-university { color: #6b7280; }
        .icon-graduation { color: #602234; }
        .icon-calendar { color: #6b7280; }
        .icon-briefcase { color: #602234; }
        .icon-building { color: #6b7280; }
        .icon-location { color: #602234; }
        /* Hide up/down arrows in table headers */
        .sort-icon { display: none !important; }
        .neumorphic {
            background: #f0f0f0;
            box-shadow: 4px 4px 10px #d1d1d1,
                       -4px -4px 10px #ffffff;
        }
        .neumorphic-btn {
            background: #f0f0f0;
            box-shadow: 4px 4px 8px #d1d1d1,
                       -4px -4px 8px #ffffff;
            transition: all 0.2s ease;
        }
        .neumorphic-btn:active {
            transform: scale(0.95);
        }
        .maroon-bg {
            background-color: #602234;
        }
        .maroon-text {
            color: #602234;
        }
        .hover-scale {
            transition: transform 0.2s ease;
        }
        .hover-scale:hover {
            transform: scale(1.02);
        }
        .table-header {
            position: sticky;
            top: 0;
            background: #f8f9fa;
            z-index: 10;
        }
        .table-container {
            margin-top: 1rem;
            position: relative;
            overflow: visible;
        }
        .table-row:hover {
            background-color: rgba(128, 0, 0, 0.05);
        }
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            z-index: 1000;
        }
        .modal-content {
            position: relative;
            margin: 10% auto;
            padding: 20px;
            width: 80%;
            max-width: 600px;
            border-radius: 8px;
        }
        .sort-icon {
            opacity: 0.3;
        }
        .sort-active {
            opacity: 1;
        }
        .filter-tag {
            background-color: #602234;
            color: white;
            padding: 4px 12px;
            border-radius: 16px;
            font-size: 0.875rem;
            display: inline-flex;
            align-items: center;
            margin: 4px;
        }
        .filter-tag .close {
            margin-left: 8px;
            cursor: pointer;
            padding: 2px;
        }
        .filter-tag .close:hover {
            opacity: 0.8;
        }
        .clickable {
            cursor: pointer;
            color: #602234;
        }
        .clickable:hover {
            text-decoration: underline;
        }
        @keyframes spin-slow {
            from {
                transform: rotate(0deg);
            }
            to {
                transform: rotate(360deg);
            }
        }
        .animate-spin-slow {
            animation: spin-slow 10s linear infinite;
        }
        /* Dark mode styles */
        .dark .table-header {
            background: #1a202c;
        }
        .dark .table-row:hover {
            background-color: rgba(255, 255, 255, 0.05);
        }
        .dark .neumorphic {
            background: #2d3748;
            box-shadow: 4px 4px 10px #1a202c,
                       -4px -4px 10px #4a5568;
        }
        .dark .neumorphic-btn {
            background: #2d3748;
            box-shadow: 4px 4px 8px #1a202c,
                       -4px -4px 8px #4a5568;
        }
        .dark {
            background-color: #1a202c;
            color: #f7fafc;
        }
        .dark .text-gray-600 {
            color: #cbd5e0;
        }
        .dark .bg-white {
            background-color: #2d3748;
        }
        /* Responsive table columns */
        @media (max-width: 1024px) {
            .col-major,
            .col-year {
                display: none;
            }
        }
        
        @media (max-width: 768px) {
            .col-location {
                display: none;
            }
        }
        
        @media (max-width: 640px) {
            .col-company {
                display: none;
            }
        }
        
        /* Search input styling */
        .search-input {
            background-color: white;
            border: 1px solid #e2e8f0;
            transition: all 0.3s ease;
        }
        
        .search-input:focus {
            border-color: #800000;
            box-shadow: 0 0 0 3px rgba(128, 0, 0, 0.1);
        }
        /* Table header icon colors and sizing */
        .table-header-icon {
            width: 1em;
            height: 1em;
            display: inline-block;
            vertical-align: middle;
            margin-right: 0.5em;
        }
        /* Hide up/down arrows in table headers */
        .sort-icon { display: none !important; }
        /* Filter modal redesign */
        .modal-content.nice-modal {
            background: #fff;
            border-radius: 1.25rem;
            box-shadow: 0 8px 32px rgba(60, 0, 0, 0.15);
            padding: 2rem 2.5rem;
            max-width: 480px;
            width: 95%;
            margin: 5% auto;
            max-height: 80vh;
            overflow-y: auto;
        }
        .filter-checkbox {
            accent-color: #602234;
            width: 1.1em;
            height: 1.1em;
            border-radius: 0.3em;
            margin-right: 0.5em;
            vertical-align: middle;
        }
        .filter-label {
            display: flex;
            align-items: center;
            margin-bottom: 0.5em;
            font-size: 1em;
            cursor: pointer;
        }
        .filter-group-title {
            font-weight: bold;
            margin-top: 1em;
            margin-bottom: 0.5em;
            color: #602234;
        }
        .search-filter-input {
            width: 100%;
            padding: 0.5rem;
            border: 1px solid #e2e8f0;
            border-radius: 0.375rem;
            margin-bottom: 1rem;
        }
        .search-filter-input:focus {
            outline: none;
            border-color: #602234;
            box-shadow: 0 0 0 3px rgba(96, 34, 52, 0.1);
        }
        .dynamic-checkboxes {
            margin-bottom: 1rem;
        }
        .close-button {
            background: #602234;
            color: white;
            padding: 0.5rem 1.5rem;
            border-radius: 0.375rem;
            font-weight: 500;
            transition: all 0.2s ease;
        }
        .close-button:hover {
            background-color: #4a1a28;
        }
        .neumorphic-inner {
            background: #f0f0f0;
            box-shadow: inset 1.5px 1.5px 4px #e0e0e0, inset 2.5px 2.5px 8px #f7f7f7;
        }
        .close-button {
            background: #f0f0f0;
            color: #800000;
            border: none;
            box-shadow: inset 2px 2px 6px #d1d1d1, inset -2px -2px 6px #ffffff;
            padding: 0.5rem 1.5rem;
            border-radius: 0.375rem;
            font-weight: 500;
            transition: all 0.2s ease;
        }
        .close-button:hover {
            background: #e5e5e5;
        }
        .modal-content.nice-modal, .modal-content.neumorphic {
            box-shadow: inset 6px 6px 18px #d1d1d1, inset -6px -6px 18px #ffffff;
        }
        .directory-title {
            background-size: 200% 200%;
            animation: gradient-flow 3s ease-in-out infinite;
        }
        @keyframes gradient-flow {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }
        /* Autocomplete dropdown for filter modal */
        .autocomplete-dropdown {
            position: absolute;
            z-index: 1001;
            background: #e5e7eb;
            border-radius: 0.5rem;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
            width: 100%;
            max-height: 220px;
            overflow-y: auto;
            margin-top: 0.25rem;
        }
        .autocomplete-option {
            padding: 0.5rem 1rem;
            cursor: pointer;
            transition: background 0.15s;
        }
        .autocomplete-option:hover {
            background: #f3f4f6;
        }
        @keyframes gradient-flow-btn {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }
        .animate-gradient-flow-btn {
            background-size: 200% 200%;
            animation: gradient-flow-btn 6s ease-in-out infinite;
        }
        .filter-checkbox, .column-toggle {
            transition: box-shadow 0.2s, transform 0.2s;
        }
        .filter-checkbox:checked, .column-toggle:checked {
            box-shadow: 0 0 0 4px #60223422;
            transform: scale(1.12);
        }
        /* Filter modal checkboxes alignment and style */
        #filter-modal .dynamic-checkboxes .filter-label {
            margin-left: 0 !important;
            padding-left: 0.25rem !important;
        }
        #filter-modal .filter-checkbox {
            box-shadow: none !important;
            outline: none !important;
            border: 2px solid #bbb !important;
        }
        #filter-modal .filter-checkbox:checked {
            box-shadow: none !important;
            outline: none !important;
            border: 2px solid #602234 !important;
        }
        #filter-modal .dynamic-checkboxes {
            margin-left: 0 !important;
        }
        /* Hide scrollbar for modals */
        .modal-content.nice-modal, .modal-content.neumorphic {
            scrollbar-width: none;
            -ms-overflow-style: none;
        }
        .modal-content.nice-modal::-webkit-scrollbar, .modal-content.neumorphic::-webkit-scrollbar {
            display: none;
        }
        .column-toggle {
            width: 1.5rem;
            height: 1.5rem;
            border-radius: 0.375rem;
            border: 2px solid #602234;
            appearance: none;
            -webkit-appearance: none;
            cursor: pointer;
            position: relative;
            transition: all 0.2s ease;
        }
        .column-toggle:checked {
            background-color: #602234;
        }
        .column-toggle:checked::after {
            content: '✓';
            position: absolute;
            color: white;
            font-size: 1rem;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        }
        .column-toggle:nth-child(odd) {
            border-color: #000;
        }
        .column-toggle:nth-child(odd):checked {
            background-color: #000;
        }
        .column-label {
            font-size: 1.1rem;
            font-weight: 500;
            color: #333;
            margin-left: 0.75rem;
        }
        .filter-card {
            display: inline-flex;
            align-items: center;
            padding: 0.5rem 1rem;
            border-radius: 9999px;
            margin: 0.5rem;
            font-size: 0.9rem;
            font-weight: 500;
            color: white;
        }
        .filter-card:nth-child(odd) {
            background-color: #602234;
        }
        .filter-card:nth-child(even) {
            background-color: #000;
        }
        .filter-card .close {
            margin-left: 0.75rem;
            cursor: pointer;
            font-size: 1.1rem;
            opacity: 0.8;
            transition: opacity 0.2s;
        }
        .filter-card .close:hover {
            opacity: 1;
        }
        .custom-dropdown-options {
            position: absolute;
            top: 100%;
            left: 0;
            right: 0;
            background: white;
            border: 1px solid #e5e7eb;
            border-radius: 0.5rem;
            margin-top: 0.25rem;
            max-height: 200px;
            overflow-y: auto;
            z-index: 50;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        }
        .custom-dropdown-option {
            padding: 0.5rem 1rem;
            cursor: pointer;
            transition: all 0.2s;
        }
        .custom-dropdown-option:hover {
            background-color: #602234;
            color: white;
        }
        .modal-close-btn {
            position: absolute;
            bottom: 1.5rem;
            right: 1.5rem;
            padding: 0.75rem 1.5rem;
            background-color: #602234;
            color: white;
            border-radius: 0.5rem;
            font-weight: 500;
            transition: all 0.2s;
        }
        .modal-close-btn:hover {
            background-color: #4a1a28;
        }
    </style>
</head>
<body class="min-h-screen flex flex-col dot-pattern">
    <div id="navbar-container"></div>

    <main class="container w-[90vw] mx-auto py-8 mt-16">
        <div class="w-full">
            <!-- Search and Filter Section -->
            <div class="modern-card p-6 mb-8">
                <div class="flex flex-col md:flex-row gap-4 items-center justify-between">
                    <div class="relative w-full md:w-1/2">
                        <i class="fas fa-search absolute left-3 input-icon text-gray-400"></i>
                        <input type="text" id="searchInput" class="modern-input w-full pl-10 pr-4 py-3 text-lg" placeholder="Search alumni...">
                    </div>
                    <div class="flex gap-4">
                        <button id="filterBtn" class="bg-[#602234] hover:bg-[#4a1a28] text-white font-semibold py-2 px-4 rounded-lg transition-colors flex items-center">
                            <i class="fas fa-filter mr-2"></i>
                            Filter
                        </button>
                        <button id="columnsBtn" class="bg-black hover:bg-gray-800 text-white font-semibold py-2 px-4 rounded-lg transition-colors flex items-center">
                            <i class="fas fa-columns mr-2"></i>
                            Columns
                        </button>
                    </div>
                </div>
                <!-- Active Filters -->
                <div id="activeFilters" class="flex flex-wrap mt-4"></div>
            </div>

            <!-- Directory Table -->
            <div class="modern-card p-6">
                <div class="overflow-x-auto">
                    <table class="w-full">
                        <thead>
                            <tr class="border-b-2 border-gray-200">
                                <th class="text-left py-4 px-6">
                                    <div class="flex items-center">
                                        <i class="fas fa-user text-[#602234] mr-2"></i>
                                        Name
                                    </div>
                                </th>
                                <th class="text-left py-4 px-6">
                                    <div class="flex items-center">
                                        <i class="fas fa-graduation-cap text-black mr-2"></i>
                                        School
                                    </div>
                                </th>
                                <th class="text-left py-4 px-6">
                                    <div class="flex items-center">
                                        <i class="fas fa-calendar text-[#602234] mr-2"></i>
                                        Graduation Year
                                    </div>
                                </th>
                                <th class="text-left py-4 px-6">
                                    <div class="flex items-center">
                                        <i class="fas fa-book text-[#602234] mr-2"></i>
                                        Major
                                    </div>
                                </th>
                                <th class="text-left py-4 px-6">
                                    <div class="flex items-center">
                                        <i class="fas fa-briefcase text-black mr-2"></i>
                                        Position
                                    </div>
                                </th>
                                <th class="text-left py-4 px-6">
                                    <div class="flex items-center">
                                        <i class="fas fa-building text-[#602234] mr-2"></i>
                                        Company
                                    </div>
                                </th>
                                <th class="text-left py-4 px-6">
                                    <div class="flex items-center">
                                        <i class="fas fa-map-marker-alt text-black mr-2"></i>
                                        Location
                                    </div>
                                </th>
                                <th class="text-left py-4 px-6">
                                    <div class="flex items-center">
                                        <i class="fas fa-link text-[#602234] mr-2"></i>
                                        Actions
                                    </div>
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            <% alumni.forEach(alum => { %>
                                <tr class="border-b border-gray-200 hover:bg-gray-50">
                                    <td class="py-4 px-6">
                                        <div class="flex items-center">
                                            <div class="w-10 h-10 rounded-full bg-[#602234] flex items-center justify-center text-white text-lg mr-4">
                                                <%= alum.name.split(' ').map(n => n[0]).join('') %>
                                            </div>
                                            <div>
                                                <a href="/user/<%= alum.username %>" class="font-semibold text-gray-800 hover:text-[#602234] transition-colors"><%= alum.name %></a>
                                                <div class="text-sm text-gray-500">@<%= alum.username %></div>
                                            </div>
                                        </div>
                                    </td>
                                    <td class="py-4 px-6 text-gray-600 clickable" data-filter="school"><%= alum.school || 'n/a' %></td>
                                    <td class="py-4 px-6 text-gray-600 clickable" data-filter="graduationYear"><%= alum.graduationYear || 'n/a' %></td>
                                    <td class="py-4 px-6 text-gray-600 clickable" data-filter="major"><%= alum.major || 'n/a' %></td>
                                    <td class="py-4 px-6 text-gray-600 clickable" data-filter="currentPosition"><%= alum.currentPosition || 'n/a' %></td>
                                    <td class="py-4 px-6 text-gray-600 clickable" data-filter="company"><%= alum.company || 'n/a' %></td>
                                    <td class="py-4 px-6 text-gray-600 clickable" data-filter="location"><%= alum.location || 'n/a' %></td>
                                    <td class="py-4 px-6">
                                        <div class="flex items-center space-x-4">
                                            <a href="/user/<%= alum.username %>" class="text-[#602234] hover:text-[#4a1a28] transition-colors">
                                                <i class="fas fa-eye text-xl"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </main>

    <!-- Filter Modal -->
    <div id="filterModal" class="fixed inset-0 bg-black bg-opacity-50 hidden items-center justify-center z-50">
        <div class="bg-white rounded-xl p-8 w-11/12 sm:w-3/4 md:w-2/3 lg:w-1/2 max-h-[90vh] overflow-y-auto relative">
            <div class="flex justify-between items-center mb-6">
                <h2 class="text-2xl font-bold text-[#602234]">Filter Alumni</h2>
                <button id="closeFilterModal" class="text-gray-500 hover:text-gray-700">
                    <i class="fas fa-times text-xl"></i>
                </button>
            </div>
            <div class="space-y-6">
                <!-- School Filter -->
                <div class="filter-group relative">
                    <label class="block text-sm font-medium text-gray-700 mb-2">School</label>
                    <div class="custom-dropdown">
                        <input type="text" class="filter-search w-full px-3 py-2 border border-gray-300 rounded-lg" placeholder="Search schools...">
                        <div class="custom-dropdown-options hidden"></div>
                    </div>
                </div>

                <!-- Graduation Year Filter -->
                <div class="filter-group relative">
                    <label class="block text-sm font-medium text-gray-700 mb-2">Graduation Year</label>
                    <div class="custom-dropdown">
                        <input type="text" class="filter-search w-full px-3 py-2 border border-gray-300 rounded-lg" placeholder="Search years...">
                        <div class="custom-dropdown-options hidden"></div>
                    </div>
                </div>

                <!-- Major Filter -->
                <div class="filter-group relative">
                    <label class="block text-sm font-medium text-gray-700 mb-2">Major</label>
                    <div class="custom-dropdown">
                        <input type="text" class="filter-search w-full px-3 py-2 border border-gray-300 rounded-lg" placeholder="Search majors...">
                        <div class="custom-dropdown-options hidden"></div>
                    </div>
                </div>

                <!-- Position Filter -->
                <div class="filter-group relative">
                    <label class="block text-sm font-medium text-gray-700 mb-2">Position</label>
                    <div class="custom-dropdown">
                        <input type="text" class="filter-search w-full px-3 py-2 border border-gray-300 rounded-lg" placeholder="Search positions...">
                        <div class="custom-dropdown-options hidden"></div>
                    </div>
                </div>

                <!-- Company Filter -->
                <div class="filter-group relative">
                    <label class="block text-sm font-medium text-gray-700 mb-2">Company</label>
                    <div class="custom-dropdown">
                        <input type="text" class="filter-search w-full px-3 py-2 border border-gray-300 rounded-lg" placeholder="Search companies...">
                        <div class="custom-dropdown-options hidden"></div>
                    </div>
                </div>

                <!-- Location Filter -->
                <div class="filter-group relative">
                    <label class="block text-sm font-medium text-gray-700 mb-2">Location</label>
                    <div class="custom-dropdown">
                        <input type="text" class="filter-search w-full px-3 py-2 border border-gray-300 rounded-lg" placeholder="Search locations...">
                        <div class="custom-dropdown-options hidden"></div>
                    </div>
                </div>
            </div>
            <button class="modal-close-btn">Close</button>
        </div>
    </div>

    <!-- Columns Modal -->
    <div id="columnsModal" class="fixed inset-0 bg-black bg-opacity-50 hidden items-center justify-center z-50">
        <div class="bg-white rounded-xl p-8 w-11/12 sm:w-3/4 md:w-2/3 lg:w-1/2 max-h-[90vh] overflow-y-auto relative">
            <div class="flex justify-between items-center mb-6">
                <h2 class="text-2xl font-bold text-[#602234]">Customize Columns</h2>
                <button id="closeColumnsModal" class="text-gray-500 hover:text-gray-700">
                    <i class="fas fa-times text-xl"></i>
                </button>
            </div>
            <div class="space-y-4">
                <div class="flex items-center">
                    <input type="checkbox" id="colName" class="column-toggle" checked>
                    <label for="colName" class="column-label">Name</label>
                </div>
                <div class="flex items-center">
                    <input type="checkbox" id="colSchool" class="column-toggle" checked>
                    <label for="colSchool" class="column-label">School</label>
                </div>
                <div class="flex items-center">
                    <input type="checkbox" id="colGradYear" class="column-toggle" checked>
                    <label for="colGradYear" class="column-label">Graduation Year</label>
                </div>
                <div class="flex items-center">
                    <input type="checkbox" id="colMajor" class="column-toggle" checked>
                    <label for="colMajor" class="column-label">Major</label>
                </div>
                <div class="flex items-center">
                    <input type="checkbox" id="colPosition" class="column-toggle" checked>
                    <label for="colPosition" class="column-label">Position</label>
                </div>
                <div class="flex items-center">
                    <input type="checkbox" id="colCompany" class="column-toggle" checked>
                    <label for="colCompany" class="column-label">Company</label>
                </div>
                <div class="flex items-center">
                    <input type="checkbox" id="colLocation" class="column-toggle" checked>
                    <label for="colLocation" class="column-label">Location</label>
                </div>
            </div>
            <button class="modal-close-btn">Close</button>
        </div>
    </div>

    <script type="module">
        import Navbar from '../../components/navbar.js';
        
        // Initialize navbar
        const navbar = new Navbar();
        document.getElementById('navbar-container').innerHTML = navbar.render();
        navbar.init();

        $(document).ready(function() {
            // Load column preferences from localStorage
            function loadColumnPreferences() {
                const columns = ['Name', 'School', 'GradYear', 'Major', 'Position', 'Company', 'Location'];
                columns.forEach(column => {
                    const isVisible = localStorage.getItem(`col${column}`) !== 'false';
                    $(`#col${column}`).prop('checked', isVisible);
                    const columnIndex = $(`th:contains('${column}')`).index();
                    $(`td:nth-child(${columnIndex + 1}), th:nth-child(${columnIndex + 1})`).toggle(isVisible);
                });
            }

            // Save column preferences to localStorage
            function saveColumnPreferences() {
                $('.column-toggle').each(function() {
                    localStorage.setItem($(this).attr('id'), $(this).prop('checked'));
                });
            }

            // Initialize column preferences
            loadColumnPreferences();

            // Column toggle functionality
            $('.column-toggle').on('change', function() {
                const columnId = $(this).attr('id');
                const columnIndex = columnId.replace('col', '').toLowerCase();
                const column = $(`th:contains('${columnIndex.charAt(0).toUpperCase() + columnIndex.slice(1)}')`).index();
                
                $('td:nth-child(' + (column + 1) + ')').toggle(this.checked);
                $('th:nth-child(' + (column + 1) + ')').toggle(this.checked);
                
                saveColumnPreferences();
            });

            // Initialize filter options
            function initializeFilterOptions() {
                const columns = ['school', 'graduationYear', 'major', 'currentPosition', 'company', 'location'];
                columns.forEach(column => {
                    const uniqueValues = new Set();
                    $(`td[data-filter="${column}"]`).each(function() {
                        const value = $(this).text().trim();
                        if (value !== 'n/a') {
                            uniqueValues.add(value);
                        }
                    });
                    
                    const options = Array.from(uniqueValues).sort();
                    const filterGroup = $(`.filter-group:has(label:contains('${column.charAt(0).toUpperCase() + column.slice(1)}'))`);
                    const optionsContainer = filterGroup.find('.custom-dropdown-options');
                    
                    optionsContainer.empty();
                    options.forEach(option => {
                        optionsContainer.append(`
                            <div class="custom-dropdown-option" data-value="${option}">
                                ${option}
                            </div>
                        `);
                    });
                });
            }

            // Initialize filter options on page load
            initializeFilterOptions();

            // Filter search functionality
            $('.filter-search').on('input', function() {
                const searchTerm = $(this).val().toLowerCase();
                const options = $(this).siblings('.custom-dropdown-options');
                const optionsList = options.find('.custom-dropdown-option');
                
                optionsList.each(function() {
                    const text = $(this).text().toLowerCase();
                    $(this).toggle(text.includes(searchTerm));
                });
                
                options.toggleClass('hidden', !searchTerm);
            });

            // Close dropdowns when clicking outside
            $(document).on('click', function(e) {
                if (!$(e.target).closest('.custom-dropdown').length) {
                    $('.custom-dropdown-options').addClass('hidden');
                }
            });

            // Apply filter when clicking on option
            $('.custom-dropdown-option').on('click', function() {
                const value = $(this).data('value');
                const column = $(this).closest('.filter-group').find('label').text().toLowerCase();
                const filterId = `${column}-${value}`;
                
                if (!$(`#filter-${filterId}`).length) {
                    $('#activeFilters').append(`
                        <div id="filter-${filterId}" class="filter-card">
                            ${column}: ${value}
                            <span class="close" data-column="${column}" data-value="${value}">×</span>
                        </div>
                    `);
                }
                
                $(this).closest('.custom-dropdown-options').addClass('hidden');
                $(this).closest('.filter-group').find('input').val('');
                
                applyFilters();
            });

            // Remove filter when clicking close button
            $('#activeFilters').on('click', '.close', function() {
                $(this).parent().remove();
                applyFilters();
            });

            // Apply all active filters
            function applyFilters() {
                const filters = {};
                $('.filter-card').each(function() {
                    const column = $(this).find('.close').data('column');
                    const value = $(this).find('.close').data('value');
                    if (!filters[column]) {
                        filters[column] = new Set();
                    }
                    filters[column].add(value);
                });

                $('tbody tr').each(function() {
                    let show = true;
                    for (const [column, values] of Object.entries(filters)) {
                        const cellValue = $(this).find(`td[data-filter="${column}"]`).text().trim();
                        if (!values.has(cellValue)) {
                            show = false;
                            break;
                        }
                    }
                    $(this).toggle(show);
                });
            }

            // Modal close buttons
            $('.modal-close-btn').on('click', function() {
                $(this).closest('.fixed').addClass('hidden').removeClass('flex');
            });

            // Clickable cell filter functionality
            $('td[data-filter]').on('click', function() {
                const column = $(this).data('filter');
                const value = $(this).text().trim();
                
                if (value !== 'n/a') {
                    const filterId = `${column}-${value}`;
                    if (!$(`#filter-${filterId}`).length) {
                        $('#activeFilters').append(`
                            <div id="filter-${filterId}" class="filter-card">
                                ${column}: ${value}
                                <span class="close" data-column="${column}" data-value="${value}">×</span>
                            </div>
                        `);
                        applyFilters();
                    }
                }
            });

            // Sort users with missing school or graduation year to bottom
            function sortUsers() {
                const rows = $('tbody tr').toArray();
                rows.sort((a, b) => {
                    const aSchool = $(a).find('td[data-filter="school"]').text().trim();
                    const bSchool = $(b).find('td[data-filter="school"]').text().trim();
                    const aYear = $(a).find('td[data-filter="graduationYear"]').text().trim();
                    const bYear = $(b).find('td[data-filter="graduationYear"]').text().trim();
                    
                    if (aSchool === 'n/a' && bSchool !== 'n/a') return 1;
                    if (aSchool !== 'n/a' && bSchool === 'n/a') return -1;
                    if (aYear === 'n/a' && bYear !== 'n/a') return 1;
                    if (aYear !== 'n/a' && bYear === 'n/a') return -1;
                    return 0;
                });
                
                $('tbody').append(rows);
            }

            // Sort users on page load
            sortUsers();
        });
    </script>
</body>
</html> 